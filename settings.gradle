dependencyResolutionManagement {
    pluginManagement {
        versionCatalogs {
            libs {
                from(files("../libs.versions.toml"))
            }
        }
    }
}

rootProject.name = 'securityapi'

